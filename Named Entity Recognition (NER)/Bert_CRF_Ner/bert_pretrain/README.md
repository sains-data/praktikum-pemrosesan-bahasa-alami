# Bobot pra-latih BERT

Unduh dan letakkan di folder ini:

- `config.json`
- `pytorch_model.bin` (atau `model.safetensors` jika memakai transformers terbaru)

`vocab.txt` sudah disertakan. Gunakan checkpoint BERT-base Chinese atau multilingual sesuai data Anda.
