Panduan lengkap: README.md di folder ini.

Ringkas:
1. corpus/corpus.txt — satu artikel per baris → python corpus/pro_data.py
2. python get_train_data.py — buat process_data0.json (MLM+NSP)
3. python run_pretrain.py — mulai pra-pelatihan
